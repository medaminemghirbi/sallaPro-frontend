import { FilterByVerificationPipe } from './filter-by-verification.pipe';

describe('FilterByVerificationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByVerificationPipe();
    expect(pipe).toBeTruthy();
  });
});
